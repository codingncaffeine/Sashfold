* text=auto
*.sh text eol=lf
*.yml text eol=lf
*.png binary
*.jpg binary
*.gif binary
# Streams and the originals they decode to, compared byte for byte.
tests/fixtures/brotli/* binary

# Browser themes as they are handed around: zips, compared byte for byte.
*.xpi binary
*.crx binary
*.zip binary

# Firefox's compressed copies of its bookmarks: a size and an LZ4 block.
*.jsonlz4 binary
