# Build output
build/
build-*/

# Local planning documents (never published)
_plans/

# Render output at the repo root (headless demo PNGs land here)
/*.png

# Editor litter
.vs/
.vscode/
.idea/
*.swp

# The Web Platform Tests checkout tools/wpt-fetch.sh makes (never committed)
/wpt/

# The Unicode data files tools/ucd-fetch.sh downloads (never committed)
/ucd/

# The two-compiler build script's logs at the repo root
/build-*.log
reftest-actual/
/test262/
/opus-vectors

# A reader's own filter lists (blocklists/README.md): read from here, never committed.
blocklists/filters/*.txt
blocklists/nefarious/*.txt
