# Build output
build/
build-*/

# Local planning documents (never published)
_plans/

# Render output at the repo root (headless demo PNGs land here)
/*.png

# Editor litter
.vs/
.vscode/
.idea/
*.swp

# The Web Platform Tests checkout tools/wpt-fetch.sh makes (never committed)
/wpt/

# The two-compiler build script's logs at the repo root
/build-*.log
reftest-actual/
