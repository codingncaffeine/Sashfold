# A surface's own tokens are read: under tokens-theme.json, which names
# them in colors nothing else wears, the toolbar, its buttons' glyphs, the
# front tab's title and line, a focused field and a menu each take theirs.
# (Left out, each follows the token it derives from — test_theme — and the
# chrome's goldens, drawn from themes that name none, hold that byte for
# byte.)
resize 800 500
open data:text/html,<title>Tokens</title><p>x</p>
assert-theme Tokens

# The toolbar apart from the tab in front, and a glyph of its menu button.
assert-pixel 300 38 #102030
assert-pixel 780 55 #c3c70c

# The front tab: its line along the top, its title.
assert-pixel 50 6 #ff00ff
assert-pixel 16 16 #00ff00

# The address bar at rest wears the field's colors; with the focus, its own.
assert-pixel 118 56 #14161b
assert-pixel 114 56 #3a404b
key ctrl+l
assert-pixel 118 56 #203040
assert-pixel 114 56 #ff8000
# Its text, all of it selected, wears the color of selected text.
assert-pixel 124 55 #ffff00
key Escape
assert-pixel 118 56 #14161b

# The line along the toolbar's top, broken where the tab in front runs into
# the toolbar.
assert-pixel 400 36 #00a0a0
assert-pixel 100 36 #102030

# A toolbar button held down wears a color of its own; let go of, with the
# pointer still on it, the pointer's.
press 88 56
assert-pixel 76 56 #a000a0
release
assert-pixel 76 56 #333944
move 400 300

# A menu: its background, and the row under the pointer.
main-menu
menu-hover 0 0
assert-pixel 600 200 #300030
assert-pixel 770 88 #006000
key Escape

# The frame of a window that is not the one in front, in a theme that says
# nothing of it — this one does not — is its frame dimmed by the rule: the
# built-in frame #1f2228 becomes #3b3f47, and is itself again in front.
window-active off
assert-pixel 600 10 #3b3f47
window-active on
assert-pixel 600 10 #1f2228

# A button's icon wears the toolbar's icon color, and the color of what
# cannot be pressed when it cannot: Back, which can, and Forward, which
# cannot, each read where its arrow's shaft covers three quarters of a pixel.
assert-pixel 20 55 #c3c70c
assert-pixel 54 55 #444c58
